# Workflows

Keep each workflow in its own folder so orchestration logic can grow without forcing unrelated edits.

Example:

- `interview-workflow/workflow.ts`
- `differential-diagnosis-workflow/workflow.ts`
