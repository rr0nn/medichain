# Tools

Organize reusable tools by scope:

- `shared/` for generic tools used across multiple agents
- domain folders such as `diagnosis/` or `patient/` for reusable tools tied to one problem space

Each real tool should live in its own folder once it has more than one file.
